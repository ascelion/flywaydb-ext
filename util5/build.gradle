
apply plugin: 'java-library'

dependencies {
	api project( ':shared' )
	compileOnly configurations.flyway5

	testImplementation project( path: ':shared', configuration: 'test' )
	testImplementation configurations.flyway5
}

apply from: rootProject.file( 'publish.gradle' )

